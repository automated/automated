import BaseFrame from '../src/components/base-frame';
import Button from '../src/components/button';

const Base = () => (
  <BaseFrame>
    <Button onClick={() => {}} />
  </BaseFrame>
);

export default Base;
